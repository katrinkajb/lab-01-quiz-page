// import functions and grab DOM elements
const quizButton = document.getElementById('quiz-button');
const resultsDisplay = document.getElementById('results-area');

// initialize state
console.log(quizButton);
console.log(resultsDisplay);

// set event listeners to update state and DOM


quizButton.addEventListener('click', () => {
    alert("let's take a quiz on what we learned!")
    confirm('Hello, would you like to take my quiz about nudibranchs?');

})